{
  "name": "grocery-bot",
  "version": "2.0.0",
  "description": "ZIPRA WhatsApp grocery ordering bot + admin dashboard (SQLite database)",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "simulate": "node scripts/simulate.js",
    "test": "node scripts/test-bot.js && node scripts/test-api.js"
  },
  "engines": {
    "node": ">=22.13.0"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "serverless-http": "^4.0.0"
  }
}